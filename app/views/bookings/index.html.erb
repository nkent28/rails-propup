<div class="banner2">
  <div class="org-box">
    <div class="order-greeting">
      <div class=text-center>
        <h1> Hello <%= current_user.first_name %> 👋 </h1>
          <p>These are your bookings:  </p>
      </div>
    </div>
  </div>
        <div class="card-body">
          <%@bookings.each do |booking|%>
            <div class="card-info">
              <% booking.prop.photos.each do |photo|%>
                <%= cl_image_tag photo.key, height: 300, width: 400, crop: :fill%>
                <%end%>
                <div class="card-infos">
                 <h1><%=booking.prop.name%></h1>
                  <p><strong>Start Date:</strong> <%=booking.start_date.strftime('%d-%m-%Y')%></p>
                  <p><strong>End Date: </strong><%=booking.end_date.strftime('%d-%m-%Y')%></p>
                  <div class="text-center">
                  <%= link_to "View", booking_path(booking), :class => "btn-booking"%>
                  </div>
                </div>
              </div>
            <%end%>
        </div>
      </div>
     </div>
    </div>
  </div>
</div>



